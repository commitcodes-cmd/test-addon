namespace myAddon {

    //% block="make player jump %sprite with power %power"
        export function playerJump(sprite: Sprite, power: number) {
                sprite.vy = -power;
                    }

                    }
