namespace myAddon {

    //% block="let %varname = %integer"
        export function let(varname: varname, integer: number) {
               console.log(integer);
                    }

                    }
